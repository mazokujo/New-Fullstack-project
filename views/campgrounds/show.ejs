<%- layout('layouts/boilerplate') %>
    <h1> Details on camp: <b>
            <%=item.title%>
        </b>
    </h1>
    <div>
        <b>
            <%=item.title%>
        </b> is a camping site located in <b>
            <%= item.location %>
        </b>

    </div>
    <div><img style=" width : 100; height : 100" src="<%=item.image%>" alt=""></div>
    <div><b>Description:</b>
        <%=item.description%>
    </div>
    <div><b>Price:</b> This camp is priced at $<%=item.price%> a week
    </div>
    <div><a href="/campground">Back to all campgrounds</a></div>
    <div><a href="/campground/<%=item._id%>/edit">Edit campground</a></div>
    <form action="/campground/<%=item._id%>?_method=DELETE" method="POST">
        <button>Delete Campground</button>
    </form>